push(bx);
push(ax);
{
    cmp(al, 0);
    jz(__ishvara_fasm_if_end_1);
    {
        if (al === _enter) {}

        
        if (al === _backspace) {}
    }
}
__ishvara_fasm_if_end_1: {
    pop(ax);
    pop(bx);
}
